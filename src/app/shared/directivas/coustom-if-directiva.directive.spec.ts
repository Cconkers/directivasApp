import { CoustomIfDirectivaDirective } from './coustom-if-directiva.directive';

describe('CoustomIfDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new CoustomIfDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
